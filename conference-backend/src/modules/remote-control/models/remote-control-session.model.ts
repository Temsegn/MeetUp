import type { RemoteControlSession } from '../types/remote-control.types';

/** Ephemeral control session — not persisted. Lifetime is the meeting only. */
export type RemoteControlSessionModel = RemoteControlSession;

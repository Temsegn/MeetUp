# Frontend — copy to .env for local Vite dev only.
# Production Docker builds with empty VITE_API_URL (same-origin nginx proxy).
VITE_API_URL=http://localhost:4001

# Public site origin for invitation, guest join, and share links (no trailing slash).
# Keep this separate from VITE_API_URL.
VITE_FRONTEND_URL=https://46.246.120.148:8981
